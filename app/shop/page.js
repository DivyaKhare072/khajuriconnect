'use client';
import {useMemo,useState} from 'react';import {useSearchParams} from 'next/navigation';import Nav from '../../components/Nav';import BookCard from '../../components/BookCard';import {books,categories} from '../../lib-data';
export default function Shop(){const params=useSearchParams();const initial=params.get('q')||'';const cat=params.get('category')||'All';const [q,setQ]=useState(initial);const [active,setActive]=useState(cat);const [toast,setToast]=useState('');const filtered=useMemo(()=>books.filter(b=>(active==='All'||b.category===active)&&(!q||`${b.title} ${b.author} ${b.category} ${b.store}`.toLowerCase().includes(q.toLowerCase()))),[q,active]);const add=b=>{setToast(`${b.title} added to your cart`);setTimeout(()=>setToast(''),2200)};return <><Nav/><main className="page"><div className="container"><div className="kicker">Khajuri marketplace</div><h1 className="page-title serif">Find your book.</h1><div className="small-search"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search title, author, category or store"/><button>Search</button></div><div className="shop-layout"><aside className="filters"><strong>Browse</strong><button className={active==='All'?'active':''} onClick={()=>setActive('All')}>All books</button>{categories.map(([,n])=><button key={n} className={active===n?'active':''} onClick={()=>setActive(n)}>{n}</button>)}</aside><section><div className="section-head"><div><div className="kicker">Showing</div><p style={{margin:0}}>{filtered.length} prototype listings</p></div></div><div className="book-grid">{filtered.map(b=><BookCard key={b.id} book={b} onAdd={add}/>)}</div></section></div></div></main>{toast&&<div className="toast">{toast}</div>}</>}
