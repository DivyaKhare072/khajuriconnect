'use client';
import {useParams} from 'next/navigation';import Link from 'next/link';import Nav from '../../../components/Nav';import {books} from '../../../lib-data';import {ArrowLeft,MapPin,ShoppingCart,Phone} from 'lucide-react';
export default function Product(){const {id}=useParams();const b=books.find(x=>x.id===id)||books[0];return <><Nav/><main className="page"><div className="container"><Link href="/shop" className="btn"><ArrowLeft size={15}/> Back to books</Link><div style={{display:'grid',gridTemplateColumns:'minmax(280px,430px) 1fr',gap:55,alignItems:'center',marginTop:45}}><div className="cover" style={{height:560,borderRadius:28,background:b.color}}><span style={{fontSize:28}}>{b.title}</span></div><div><div className="kicker">{b.category} · {b.condition}</div><h1 className="page-title serif" style={{fontSize:'clamp(42px,6vw,76px)'}}>{b.title}</h1><p style={{fontSize:18,color:'var(--muted)'}}>{b.author}</p><div style={{fontSize:32,fontWeight:800,margin:'25px 0'}}>₹{b.price}</div><div className="seller-panel"><strong>{b.store}</strong><p className="meta"><MapPin size={13}/> Khajuri Bazaar · Available now</p><div style={{display:'flex',gap:10,marginTop:18}}><button className="btn dark"><ShoppingCart size={16}/> Add to cart</button><button className="btn"><Phone size={16}/> Contact seller</button></div></div></div></div></div></main></>}
