'use client';
import Nav from '../../components/Nav';import Link from 'next/link';import {MapPin,ArrowRight} from 'lucide-react';import {stores} from '../../lib-data';
export default function Stores(){return <><Nav/><main className="page"><div className="container"><div className="kicker">Local storefronts</div><h1 className="page-title serif">Meet Khajuri.</h1><p className="hero-copy" style={{margin:'0 0 45px 0'}}>Explore the stores that make the market what it is. This prototype uses sample listings to demonstrate the future marketplace.</p><div className="category-grid">{stores.map((s,i)=><article className="category" key={s.name}><div className="emoji">🏪</div><h3>{s.name}</h3><span>{s.tag}<br/>{s.books} books · ★ {s.rating}<br/><MapPin size={13}/> {s.distance}</span><Link className="btn" style={{position:'absolute',bottom:18,left:18}} href="/shop">View inventory <ArrowRight size={14}/></Link></article>)}</div></div></main></>}
